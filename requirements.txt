# Core dependencies (mirrors pyproject.toml)
# Install with: pip install -r requirements.txt
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
redis>=5.0.0
celery>=5.3.0
httpx>=0.27.0

# Dev mode (SQLite via run_dev.py)
aiosqlite>=0.20.0